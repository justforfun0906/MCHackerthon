# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build artifacts (we'll build directly to root for GitHub Pages)
dist/

# Keep source files and built files separate
# The built files will be in root for GitHub Pages
/assets/
# But keep the index.html from build in root